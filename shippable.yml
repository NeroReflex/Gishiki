language: php

php:
  - 5.4

before_script:
  - mkdir -p shippable/testresults

script:
 - composer self-update
 - composer install
 - phpunit --log-junit shippable/testresults/junit.xml

notifications:
  email: false
